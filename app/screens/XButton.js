import React from "react";
import { Button } from "react-native";

const XButton = () => {
  return (
    <Button
      title="X"
      onPress={() => {
       
      }}
    />
  );
};


export default XButton;
